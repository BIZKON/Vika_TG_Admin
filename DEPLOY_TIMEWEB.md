# Деплой на Timeweb Cloud

Пошаговая инструкция по запуску Vika TG Admin на Timeweb Cloud.

---

## Часть 1: Регистрация и создание сервера

### 1.1 Регистрация

1. Перейдите на **https://timeweb.cloud**
2. Нажмите **"Регистрация"** (правый верхний угол)
3. Введите email и придумайте пароль
4. Подтвердите email (придёт письмо)
5. Пополните баланс (минимум 300₽, принимают карты РФ)

### 1.2 Создание сервера

1. В личном кабинете нажмите **"Облачные серверы"** → **"Создать"**

2. **Выберите конфигурацию:**

```
┌─────────────────────────────────────────────────────────────┐
│  НАСТРОЙКИ СЕРВЕРА                                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Операционная система: Ubuntu 22.04 LTS                     │
│                        ▲                                    │
│                        └── ВАЖНО: именно эту версию!        │
│                                                             │
│  Регион: Россия, Москва (или Санкт-Петербург)               │
│                                                             │
│  Тариф: Cloud 1 (~300₽/мес)                                 │
│         • 1 CPU                                             │
│         • 1 GB RAM                                          │
│         • 15 GB SSD                                         │
│                                                             │
│  Или лучше: Cloud 2 (~500₽/мес)                             │
│         • 1 CPU                                             │
│         • 2 GB RAM  ← рекомендую для RAG                    │
│         • 30 GB SSD                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

3. **Имя сервера:** `vika-bot` (или любое другое)

4. **SSH-ключ:**
   - Если не знаете что это — пропустите, будет пароль
   - Если хотите настроить — см. раздел "SSH-ключи" ниже

5. Нажмите **"Создать сервер"**

### 1.3 Получение данных доступа

После создания (1-2 минуты) вы увидите:

```
IP-адрес: 185.XXX.XXX.XXX
Логин: root
Пароль: ваш_сгенерированный_пароль
```

📝 **Запишите эти данные!** Пароль показывается только один раз.

---

## Часть 2: Подключение к серверу

### 2.1 Windows 10/11

**Вариант А: Через Windows Terminal (рекомендую)**

1. Откройте **Windows Terminal** (найдите в поиске)
2. Введите команду:

```bash
ssh root@185.XXX.XXX.XXX
```

3. На вопрос `Are you sure...` напишите `yes` и нажмите Enter
4. Введите пароль (символы не отображаются — это нормально!)

**Вариант Б: Через PuTTY**

1. Скачайте PuTTY: https://putty.org/
2. Запустите `putty.exe`
3. В поле **Host Name** введите ваш IP
4. Нажмите **Open**
5. Логин: `root`, затем пароль

### 2.2 Mac / Linux

Откройте Terminal и введите:

```bash
ssh root@185.XXX.XXX.XXX
```

### 2.3 Успешное подключение

Если видите что-то типа:

```
Welcome to Ubuntu 22.04.3 LTS
root@vika-bot:~#
```

Поздравляю! Вы на сервере. 🎉

---

## Часть 3: Настройка сервера

Копируйте команды и вставляйте в терминал (правой кнопкой мыши или Ctrl+Shift+V).

### 3.1 Обновление системы

```bash
apt update && apt upgrade -y
```

Подождите завершения (может спросить `Y/n` — нажмите `y` и Enter).

### 3.2 Установка необходимых программ

```bash
apt install -y python3.11 python3.11-venv python3-pip git nano htop
```

### 3.3 Проверка Python

```bash
python3.11 --version
```

Должно показать: `Python 3.11.x`

### 3.4 Создание пользователя (безопаснее чем root)

```bash
adduser vika --disabled-password --gecos ""
usermod -aG sudo vika
```

### 3.5 Настройка sudo без пароля (для удобства)

```bash
echo "vika ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers
```

### 3.6 Переключение на пользователя vika

```bash
su - vika
```

Приглашение изменится на: `vika@vika-bot:~$`

---

## Часть 4: Установка проекта

### 4.1 Клонирование репозитория

```bash
git clone https://github.com/BIZKON/Vika_TG_Admin.git
cd Vika_TG_Admin
```

### 4.2 Создание виртуального окружения Python

```bash
python3.11 -m venv venv
source venv/bin/activate
```

После активации в начале строки появится `(venv)`:
```
(venv) vika@vika-bot:~/Vika_TG_Admin$
```

### 4.3 Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Это займёт несколько минут. Дождитесь завершения.

### 4.4 Создание папок для данных

```bash
mkdir -p data/sessions data/documents data/vector_db
```

---

## Часть 5: Получение API ключей

### 5.1 Telegram API (для ваших аккаунтов)

1. Откройте **https://my.telegram.org** в браузере
2. Введите номер телефона вашего Telegram аккаунта
3. Введите код из Telegram
4. Нажмите **"API development tools"**
5. Заполните форму:
   - App title: `Vika Bot` (любое)
   - Short name: `vikabot` (любое, без пробелов)
   - Platform: `Desktop`
6. Нажмите **"Create application"**
7. Запишите:
   - **App api_id:** `12345678`
   - **App api_hash:** `abcdef1234567890abcdef1234567890`

> ⚠️ Если у вас два аккаунта — повторите для второго

### 5.2 Создание Telegram бота

1. Откройте Telegram
2. Найдите **@BotFather** и начните диалог
3. Отправьте `/newbot`
4. Введите имя бота: `Vika Assistant` (любое)
5. Введите username: `vika_assistant_bot` (уникальный, заканчивается на `bot`)
6. Скопируйте токен:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```

### 5.3 Узнать свой User ID

1. Откройте Telegram
2. Найдите бота **@getmyid_bot**
3. Нажмите Start
4. Скопируйте **Your user ID:** `123456789`

### 5.4 Создать хаб-группу и узнать её ID

1. Создайте **новую группу** в Telegram
2. Назовите её, например: `Vika Hub`
3. Добавьте в группу вашего бота (которого создали)
4. Сделайте бота **администратором** группы
5. Добавьте бота **@getmyid_bot** в группу
6. Он напишет ID группы (начинается с `-100...`)
7. Скопируйте этот ID
8. Можете удалить @getmyid_bot из группы

### 5.5 OpenAI API Key (для RAG)

1. Перейдите на **https://platform.openai.com**
2. Зарегистрируйтесь / войдите
3. Перейдите в **API Keys** (меню слева)
4. Нажмите **"Create new secret key"**
5. Скопируйте ключ (начинается с `sk-...`)

> 💰 Стоимость: ~$0.02 за 1 миллион токенов (очень дёшево)

### 5.6 Anthropic API Key (для Claude)

1. Перейдите на **https://console.anthropic.com**
2. Зарегистрируйтесь / войдите
3. Перейдите в **API Keys**
4. Создайте новый ключ
5. Скопируйте (начинается с `sk-ant-...`)

---

## Часть 6: Настройка конфигурации

### 6.1 Создание файла .env

На сервере выполните:

```bash
nano .env
```

### 6.2 Вставьте конфигурацию

Замените значения на свои:

```bash
# ═══════════════════════════════════════════
# TELEGRAM АККАУНТЫ
# ═══════════════════════════════════════════

# Рабочий аккаунт
ACCOUNT_WORK_API_ID=12345678
ACCOUNT_WORK_API_HASH=abcdef1234567890abcdef1234567890
ACCOUNT_WORK_PHONE=+79001234567

# Личный аккаунт (если нет — оставьте как есть)
ACCOUNT_PERSONAL_API_ID=0
ACCOUNT_PERSONAL_API_HASH=
ACCOUNT_PERSONAL_PHONE=

# ═══════════════════════════════════════════
# БОТ
# ═══════════════════════════════════════════

BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
HUB_CHAT_ID=-1001234567890
MODERATOR_USER_ID=123456789

# ═══════════════════════════════════════════
# AI
# ═══════════════════════════════════════════

AI_ENABLED=true
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# ═══════════════════════════════════════════
# ДОПОЛНИТЕЛЬНО
# ═══════════════════════════════════════════

REPLY_DELAY_SECONDS=1
LOG_LEVEL=INFO
```

### 6.3 Сохранение файла

1. Нажмите **Ctrl+O** (сохранить)
2. Нажмите **Enter** (подтвердить имя файла)
3. Нажмите **Ctrl+X** (выйти из редактора)

### 6.4 Проверка

```bash
cat .env
```

Должен показать ваш конфиг.

---

## Часть 7: Первый запуск

### 7.1 Авторизация аккаунтов

```bash
source venv/bin/activate
python setup.py
```

Помощник попросит:
1. Ввести код из Telegram (придёт в приложение)
2. Если есть 2FA — ввести пароль

### 7.2 Тестовый запуск

```bash
python main.py
```

Если всё настроено правильно, увидите:
```
INFO: Конфигурация загружена
INFO: Бот запущен
INFO: Аккаунт work подключен
INFO: Мониторинг активен
```

### 7.3 Проверка в Telegram

1. Откройте хаб-группу
2. Отправьте `/help`
3. Бот должен ответить списком команд

### 7.4 Остановка

Нажмите **Ctrl+C** для остановки бота.

---

## Часть 8: Автозапуск (чтобы работал постоянно)

### 8.1 Создание сервиса systemd

```bash
sudo nano /etc/systemd/system/vika-bot.service
```

### 8.2 Вставьте конфигурацию сервиса

```ini
[Unit]
Description=Vika TG Admin Bot
After=network.target

[Service]
Type=simple
User=vika
WorkingDirectory=/home/vika/Vika_TG_Admin
Environment=PATH=/home/vika/Vika_TG_Admin/venv/bin
ExecStart=/home/vika/Vika_TG_Admin/venv/bin/python main.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### 8.3 Сохраните (Ctrl+O, Enter, Ctrl+X)

### 8.4 Активация сервиса

```bash
sudo systemctl daemon-reload
sudo systemctl enable vika-bot
sudo systemctl start vika-bot
```

### 8.5 Проверка статуса

```bash
sudo systemctl status vika-bot
```

Должно показать:
```
● vika-bot.service - Vika TG Admin Bot
     Loaded: loaded
     Active: active (running) ← ЭТО ВАЖНО
```

Нажмите **q** чтобы выйти.

---

## Часть 9: Полезные команды

### Управление ботом

```bash
# Статус
sudo systemctl status vika-bot

# Перезапуск
sudo systemctl restart vika-bot

# Остановка
sudo systemctl stop vika-bot

# Запуск
sudo systemctl start vika-bot
```

### Просмотр логов

```bash
# Последние 50 строк
sudo journalctl -u vika-bot -n 50

# Логи в реальном времени (Ctrl+C для выхода)
sudo journalctl -u vika-bot -f

# Логи за сегодня
sudo journalctl -u vika-bot --since today
```

### Обновление бота

```bash
cd /home/vika/Vika_TG_Admin
git pull
sudo systemctl restart vika-bot
```

---

## Часть 10: Загрузка документов для RAG

### 10.1 Через веб-интерфейс Timeweb (SFTP)

1. В панели Timeweb найдите раздел **"Файловый менеджер"**
2. Или используйте любой SFTP-клиент:
   - **FileZilla** (бесплатный): https://filezilla-project.org/
   - **WinSCP** (Windows): https://winscp.net/

Настройки подключения:
```
Хост: ваш IP
Порт: 22
Пользователь: vika
Пароль: (такой же как у root или настройте отдельно)
```

3. Загрузите файлы в папку:
```
/home/vika/Vika_TG_Admin/data/documents/
```

### 10.2 Через командную строку (с вашего компьютера)

**Windows (PowerShell):**
```powershell
scp C:\Users\Вы\Documents\курс.pdf vika@185.XXX.XXX.XXX:/home/vika/Vika_TG_Admin/data/documents/
```

**Mac/Linux:**
```bash
scp ~/Documents/курс.pdf vika@185.XXX.XXX.XXX:/home/vika/Vika_TG_Admin/data/documents/
```

### 10.3 Индексация документов

После загрузки файлов отправьте в хаб-группу Telegram:

```
/kb_upload
```

Бот ответит сколько документов загружено.

Затем:

```
/kb_index
```

Готово! Теперь AI использует ваши документы для ответов.

---

## Решение проблем

### Бот не запускается

```bash
# Посмотреть ошибки
sudo journalctl -u vika-bot -n 100

# Частые причины:
# 1. Неверный .env — проверьте все ключи
# 2. Не установлены зависимости:
cd /home/vika/Vika_TG_Admin
source venv/bin/activate
pip install -r requirements.txt
```

### Telegram просит код снова

```bash
# Удалите сессии и авторизуйтесь заново
rm -rf /home/vika/Vika_TG_Admin/data/sessions/*
cd /home/vika/Vika_TG_Admin
source venv/bin/activate
python setup.py
sudo systemctl restart vika-bot
```

### Ошибка "Permission denied"

```bash
sudo chown -R vika:vika /home/vika/Vika_TG_Admin
```

### Не хватает памяти (для RAG)

Если на тарифе Cloud 1 (1GB RAM) возникают проблемы:
1. Перейдите на Cloud 2 (2GB RAM) в панели Timeweb
2. Или отключите AI: в `.env` поставьте `AI_ENABLED=false`

---

## Чеклист ✅

- [ ] Сервер создан на Timeweb
- [ ] Подключились по SSH
- [ ] Python 3.11 установлен
- [ ] Проект склонирован
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] API ключи получены (Telegram, OpenAI, Anthropic)
- [ ] Файл .env заполнен
- [ ] Telegram аккаунты авторизованы
- [ ] Бот добавлен в хаб как админ
- [ ] Тестовый запуск успешен
- [ ] Systemd сервис создан и работает
- [ ] Команда /help отвечает в хабе

---

## Стоимость

| Компонент | Стоимость |
|-----------|-----------|
| Timeweb Cloud 2 | ~500₽/мес |
| OpenAI API | ~$1-5/мес (зависит от использования) |
| Anthropic API | ~$5-20/мес (зависит от использования) |
| **Итого** | **~1000-2000₽/мес** |

---

Удачного деплоя! 🚀
